# tutedude_assignments
Within the assignment_1 is two separate file which are intended to solve both task's separately.

Task_1.py : This file is dedicated to the task of Basic Mathematics Operations.

Variables : first_number, second_number (both are used for input's)
    
Operations : Addition, Subtraction, Multiplication, Division
   
Output : All the operation's are indicated between both input variables considering first_number is first number and second_number is second number within the operations.


Task_2.py : this file is dedicated to the task of Personal Greeting.

Variables : first_name, last_name, full_name.

Operation : String concatenation between first_name and last_name with a space assigned to variable full name.

Output: The full name has used to create the output.
                "Hello, (full_name)! welcome to python programm."